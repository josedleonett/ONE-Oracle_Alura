function encriptar(accion) {
  event.preventDefault();

  document.getElementById("articuloSinResultado").style.display = "none";

  document.getElementById("articuloResultado").style.display = "flex";

  if (accion === "encriptar") {
    //funcion ENCRIPTAR
  } else {
    //funcion DESENCRIPTAR
  }
}
